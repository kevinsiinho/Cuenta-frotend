import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Tu.Cuenta.SoftK',
  appName: 'SotfK',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
